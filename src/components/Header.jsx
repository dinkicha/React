export default function Header() {
    return (
        <div>
      <h1>My first react app</h1>
    </div>
    );
}

// export default Header; -- it can be use the both versions
// export default function 'Name of the function' () {something}